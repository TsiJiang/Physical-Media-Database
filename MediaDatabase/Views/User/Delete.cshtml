@model User

@{ 
    ViewBag.Title = "Delete User?";
}
<body>
    <h3>Email:    @Model.Email </h3>
    <h3>Password: &#183&#183&#183&#183&#183&#183&#183&#183</h3>
    <h1> Are you sure? </h1>
    <form>
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="Email" />
        <input type="hidden" asp-for="Password" />
        <input type="hidden" name="id" value=@Model.Id />
        <table>
            <tr>
                <th>Confirm Password: </th>
                <td><input type="password" name="pass" TextMode="Password" /></td>
            </tr>
            <tr>
                <td>
                    <button asp-controller="User" asp-action="Delete" formmethod="post" asp-route-id=@Model.Id> Delete </button>
                </td>
                <td>
                    <button asp-controller="User" asp-action="Detail" asp-route-id=@Model.Id> Cancel </button>
                </td>
            </tr>
        </table>
    </form>
</body>
</html>
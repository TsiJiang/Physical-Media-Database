# Physical-Media-Database
Contains an application that currently creates a database on your local machine with mock data, adds a webserver on your local machine and allows for full CRUD on two Tables
